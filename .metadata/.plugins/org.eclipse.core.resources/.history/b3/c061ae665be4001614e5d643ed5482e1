package week02

object rationals {
  val x = new Rational(1, 3)                      //> x  : week02.Rational = 1/2
  x.numer                                         //> res0: Int = 1
  x.denom                                         //> res1: Int = 2
  
  val y = new Rational(5, 7)                      //> y  : week02.Rational = 2/3
  val z = new Rational(3, 2)                      //> res2: week02.Rational = 7/6
}

class Rational(x: Int, y: Int) {
  def numer = x
  def denom = y

  def add(that: Rational) =
    new Rational(
    numer * that.denom + that.numer * denom,
    denom * that.denom)
    
  def sub(that: Rational) =
  new Rational(
    numer * that.denom + that.numer * denom,
    denom * that.denom)
  
  override def toString = numer + "/" + denom
  	
  def neg(r: Rational) = new Rational(-r.numer, r.denom)
}