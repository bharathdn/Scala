package week02

object rationals {
  val v = new Rational(1, 2)
  v.numer
  v.denom
}

class Rational(x: Int, y: Int) {
  def numer = x
  def denom = y

  def add(that: Rational) =
    new Rational(
    numer * that.numer + denom * that.denom,
    denom * that.denom)
}